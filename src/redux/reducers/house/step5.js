import * as CONSTANTS from '../../constants'
import initialState from '../../initialState'

const step5 = (state = initialState.house.step5, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default step5